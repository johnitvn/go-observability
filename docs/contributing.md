# Contributing

See repository-level `CONTRIBUTING.md` for contribution guidelines.

Docs-specific notes:

- Use the `docs/` folder for content.
- Preview locally with `mkdocs serve`.
- Open a PR to `main` (or target docs branch) when ready.
